<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.media.MediaView?>

<StackPane xmlns:fx="http://javafx.com/fxml" fx:controller="com.ctrlaltquest.ui.controllers.LoginController"
           stylesheets="/styles/auth.css" prefWidth="1280" prefHeight="720">
    <MediaView fx:id="backgroundVideo"
               fitWidth="1280"
               fitHeight="720"
               preserveRatio="false" />

    <BorderPane styleClass="login-layout">
        <top>
            <HBox alignment="TOP_RIGHT" spacing="12" styleClass="top-actions">
                <Button text="⚙" styleClass="icon-button" />
                <ToggleButton fx:id="muteButton" text="🔊" onAction="#handleToggleMute" styleClass="icon-button" />
            </HBox>
        </top>
        <left>
            <VBox spacing="18" styleClass="sidebar" prefWidth="300">
                <HBox alignment="CENTER_LEFT" spacing="10">
                    <ImageView fx:id="sidebarLogo"
                               fitHeight="36"
                               preserveRatio="true" />
                    <ImageView fitHeight="36"
                               preserveRatio="true"
                               image="@/assets/images/logo.png" />
                    <Label text="Contacta al creador" styleClass="sidebar-title" />
                </HBox>

                <VBox spacing="12" styleClass="news-card">
                    <Label text="News" styleClass="news-title" />
                    <Label text="Información y una contraseñade la actividad que anunciar sobre imporrtes de su cuenta bols."
                           wrapText="true"
                           styleClass="news-body" />
                </VBox>

                <VBox spacing="12" styleClass="news-card">
                    <Label text="Anuncios" styleClass="news-title" />
                    <Label text="Agrega a mis encuentros con miemrla aparicisa y la vefendia a prai."
                           wrapText="true"
                           styleClass="news-body" />
                </VBox>

                <VBox spacing="12" styleClass="news-card">
                    <Label text="Notas" styleClass="news-title" />
                    <Label text="Aumenta esta sesión, o dado semmio el olvidoesoda pon ugernino."
                           wrapText="true"
                           styleClass="news-body" />
                </VBox>
            </VBox>
        </left>
        <center>
            <VBox alignment="CENTER" spacing="16" styleClass="auth-panel">
                <Label text="Bienvenido aventurero" styleClass="auth-title" />
                <TextField fx:id="usernameField" promptText="Usuario" />
                <PasswordField fx:id="passwordField" promptText="Contraseña" />
                <Button text="Iniciar sesión" onAction="#handleLogin" styleClass="primary-button" />
                <HBox alignment="CENTER" spacing="20">
                    <Button text="Crear cuenta" onAction="#handleGoToRegister" styleClass="link-button" />
                    <Button text="Olvidé mi contraseña" onAction="#handleForgotPassword" styleClass="link-button" />
                </HBox>
            </VBox>
        </center>
    </BorderPane>
</StackPane>
